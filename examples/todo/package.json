{
  "name": "fics-todo",
  "version": "0.1.0",
  "scripts": {
    "build:packages": "cd ../../packages && rm -rf dist && bun run build && cd ../examples/todo",
    "dev": "bun run build:packages && bunx --bun vite",
    "build": "bun run build:packages && bunx --bun vite build",
    "preview": "bunx --bun vite preview",
    "serve": "bun run build && bunx --bun vite preview --host"
  },
  "dependencies": {
    "ficsjs": "*",
    "lucide-static": "1.25.0"
  },
  "devDependencies": {
    "vite": "8.1.5"
  }
}
