{
  "name": "fics_todo",
  "version": "0.1.0",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun vite build",
    "preview": "bunx --bun vite preview"
  },
  "devDependencies": {
    "tsconfig-paths": "^4.2.0",
    "vite": "^6.1.0"
  }
}