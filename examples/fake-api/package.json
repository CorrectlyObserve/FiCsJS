{
  "name": "fics-fake-api",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build:packages": "cd ../../packages && rm -rf dist && bun run build && cd ../examples/fake-api",
    "build:tailwindcss": "bunx tailwindcss --input src/global.css --output src/.tailwindcss.txt --minify",
    "dev:js": "bun build src/{,**/}*.ts --outdir dist --splitting --hot --entry-naming [name].[ext] --chunk-naming [name]-[hash].[ext]",
    "dev": "bun run build:packages && bun run build:tailwindcss && bun run dev:js &&  bun run --hot app.ts",
    "build:js": "bun build src/{,**/}*.ts --outdir dist --splitting --minify --entry-naming [name].[ext] --chunk-naming [name]-[hash].[ext]",
    "build": "rm -rf dist && bun run build:packages && bun run build:tailwindcss && bun run build:js && bun run dev:tailwind && bun run --minify app.ts"
  },
  "dependencies": {
    "ficsjs": "*",
    "@hono/node-server": "^1.13.8",
    "hono": "^4.7.1"
  },
  "devDependencies": {
    "@types/bun": "^1.2.5",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.3",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "3.4.17"
  }
}
