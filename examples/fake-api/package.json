{
  "name": "fics-fake-api",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "init": "rm -rf dist && bunx tsc && bunx postcss src/**/*.css --dir dist",
    "dev": "bun run init && bun run --hot app.ts"
  },
  "dependencies": {
    "ficsjs": "*",
    "@hono/node-server": "^1.13.8",
    "hono": "^4.7.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.2",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "3.4.17",
    "tsconfig-paths": "^4.2.0"
  }
}
