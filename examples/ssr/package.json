{
  "name": "fics-ssr",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:packages": "rm -rf dist && cd ../../packages && rm -rf dist && bun run build && cd ../examples/ssr",
    "build:css": "bunx postcss \"src/css/*.css\" \"!src/css/tailwind.css\" --dir dist --minify && bunx @tailwindcss/cli --input src/css/tailwind.css --output src/.tailwindcss.txt --minify",
    "dev:scripts": "bun build .fics/routing/entries/*.ts --outdir dist --entry-naming [name].[ext] --splitting --chunk-naming [hash].[ext]",
    "clean:djs": "rm -f dist/*.d.js",
    "dev": "bun run build:packages && fics-routes && bun run build:css && bun run dev:scripts && bun run clean:djs && bun run --hot app.ts",
    "build:scripts": "bun build src/pages/*/script.ts --outdir dist --splitting --minify --entry-naming [dir].[ext] --chunk-naming [hash].[ext]",
    "build": "bun run build:packages && fics-routes && bun run build:css && bun run build:scripts && bun run clean:djs",
    "start": "bun run --minify app.ts"
  },
  "dependencies": {
    "ficsjs": "*",
    "@hono/node-server": "2.0.11",
    "hono": "4.12.31",
    "lucide-static": "1.25.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "4.3.3",
    "@types/bun": "1.3.14",
    "cssnano": "8.0.2",
    "postcss-cli": "11.0.1",
    "tailwindcss": "4.3.3"
  }
}
