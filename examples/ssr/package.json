{
  "name": "fics-ssr",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:packages": "rm -rf dist && cd ../../packages && rm -rf dist && bun run build && cd ../examples/ssr",
    "build:css": "bunx postcss \"src/css/*.css\" \"!src/css/tailwind.css\" --dir dist --minify && bunx @tailwindcss/cli --input src/css/tailwind.css --output src/.tailwindcss.txt --minify",
    "dev:src": "bun build src/{,**}/*.ts --outdir dist --hot --entry-naming [name].[ext]",
    "dev:pages": "bun build src/pages/**/index.ts --outdir dist --hot --entry-naming [dir].[ext]",
    "clean:djs": "rm -f dist/*.d.js",
    "dev": "bun run build:packages && bun run build:css && bun run dev:src && bun run dev:pages && bun run clean:djs && bun run --hot app.ts",
    "build:src": "bun build src/{,**}/*.ts --outdir dist --splitting --minify --entry-naming [hash].[ext] --chunk-naming [hash].[ext]",
    "build:pages": "bun build src/pages/**/index.ts --outdir dist --splitting --minify --entry-naming [hash].[ext] --chunk-naming [hash].[ext]",
    "build": "bun run build:packages && bun run build:css && bun run build:src && bun run build:pages && bun run clean:djs && bun run --minify app.ts"
  },
  "dependencies": {
    "ficsjs": "*",
    "@hono/node-server": "^2.0.11",
    "hono": "^4.11.9",
    "lucide-static": "^0.510.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.18",
    "@types/bun": "^1.3.9",
    "cssnano": "^7.1.2",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^4.1.18"
  }
}
